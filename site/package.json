{
  "name": "ai-quantum-ethics-standards-site",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync_docs.mjs",
    "dev": "npm run sync && astro dev --host 0.0.0.0 --port 4321",
    "build": "npm run sync && astro build",
    "preview": "astro preview --host 0.0.0.0 --port 4321"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.32.0",
    "astro": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
